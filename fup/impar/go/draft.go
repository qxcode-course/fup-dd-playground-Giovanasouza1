package main

import (
	"fmt"
	"text/template/parse"
)
func main() {
    var n, da, db int
    fmt.Scan(&n, &da, &db)
    if da + db == par{
        fmt.Println()
    }   }
    fmt.Println("Hello, World!")
}
