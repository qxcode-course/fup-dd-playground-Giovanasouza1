package main
import "fmt"
func main() {
    var L, N int
    fmt.Scan(&L, &N)

    jogadas := make([]int, N)

    for i := 0; i < N; i++ {
        fmt.Scan(&jogadas[i])
    }   


    dist := jogadas[i]
     for i := 0; i < N; i++ {
        if jogadas[i] 
        }
    }
}