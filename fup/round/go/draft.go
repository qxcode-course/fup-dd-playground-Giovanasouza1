package main
import "fmt"
func main() {
    var n1, carac float64
    fmt.Scan(&carac, &n1)
    if carac == r {
            int round(float64 num){
        return;
    }
    } if carac == f {

    }
}
