package main
import "fmt"
func main() {
    var p, s, e int 
    fmt.Scan(&p, &s, &e)

    for {
        if s - e > p && {
            fmt.Println(s)
            break
        } else if s - e {
            fmt.Println()
        }
    }
    fmt.Println("Hello, World!")
}