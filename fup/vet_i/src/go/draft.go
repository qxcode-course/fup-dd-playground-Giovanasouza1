
package main
import "fmt"
    func main() {
    var n int
    fmt.Scan(&n)
        
    var numeros int

    for _; i <= n; i++ {
        var valor int
        fmt.Scan(&valor)
        numeros = append(numeros, valor)
    }
    fmt.Println(numeros)
    }
    